package org.yocto.sdk.remotetools.remote;

import java.util.Map;

public class RemoteStreamManager {
/*	private static Map <String, IRemoteStream> streams;
	
	public static void add(String id, IRemoteStream stream)
	{
		streams.put(id,stream);
	}
	
	public static void dispose(String id)
	{
		IRemoteStream stream=streams.remove(id);
		if(stream!=null)
		{
			stream.dispose();
		}
	}
	*/
}
